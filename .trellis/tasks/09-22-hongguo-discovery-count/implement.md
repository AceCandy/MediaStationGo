# 执行计划

1. 审核现有分类/榜单写入及调用方，加入真实首次发现 ID 结果并调整任务新增计数；用重复扫描、跨来源测试验证。
2. 定期刷新及自动失败重试筛掉已完结作品，保留未完结作品的 24 小时重抓、首次补齐、失败重试与指定 ID 手动刷新；以服务测试验证。
3. 增加详情新旧内容比较及新增/更新/无变动日志与指标，确保时间戳变化不冒充资料更新，验证重复刷新无变化与实际字段变化。
4. 更新红果契约和任务定义的旧描述，独立审查 diff、检查任务指标及工作区未提交改动；有条件时执行相关 Go 单测（不运行全量编译）。

风险点：首次发现结果应依据数据库插入事实，而不是仅凭预查询；保存摘要、榜单及检查点的事务边界不可拆散。回滚点为本任务修改的服务/仓储/契约文件，不触碰已有用户改动。

## 验证记录（2026-09-23）

- 已完成独立差异复核，补正快照比较的长数字精度：相邻 19 位分集 ID 的回归测试先复现误报无变化，再修复通过；同时覆盖封面变化和重复快照。
- 隔离 PostgreSQL 17 下执行 `go test ./internal/hongguo ./internal/repository ./internal/service -run 'Test(HongGuo(Discovery|Refresh|NotFound|Wakeup|Search|Detail|Album)|CategoryCompletion|RankUses)' -count=1 -timeout=180s`，三个包均通过，数据库用例实际执行。
- `go vet ./internal/hongguo ./internal/repository ./internal/service` 与 `git diff --check` 通过。
- 本轮临时数据库容器已关闭并自动删除；未连接生产数据库，未部署，未进行浏览器或真实上游联调。
- 保留现有完结判定：上游缺乏可靠完结证据时仍按未完结处理。
- 实现及验证完成，用户已确认提交并归档。
