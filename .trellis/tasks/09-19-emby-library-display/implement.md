# 实施与验证计划

1. 实施前读取 frontend design-system、数据库和跨层规范，确认现有弹窗、设置 API 与 framer-motion 排序用法。
2. 新增最小展示配置逻辑与写入验证，接入 Views / VirtualFolders；测试持久化、排序、隐藏、默认、新增、删除、全隐藏及权限交集。
3. 新增管理员展示设置弹窗与入口；验证草稿取消、保存失败、拖拽、上下移动、显隐和重新打开。
4. 同步 Emby API 目录中受影响的返回行为说明。
5. 运行针对性 Go 测试、`cd web && npm run lint`、`cd web && npm run build`、`git diff --check`。
6. 浏览器检查桌面和窄屏弹窗、键盘操作；如启动调试服务，结束时关闭。缺少运行环境或真实 Emby 客户端时明确报告限制。
7. 实现后独立复核本任务 diff、权限保护及所有媒体库入口的一致性。清理本任务临时产物，不提交用户无关修改。

当前状态：用户已批准实施，功能代码已完成。未提交代码，未修改现有媒体库数据。

## 验证记录

- `TestEmbyLibraryDisplay`：默认、排序、新增、删除、全部隐藏及非法格式校验通过。
- `TestEmbyLibraryDisplayPersistenceAndEntries`：独立临时 PostgreSQL 17 上执行通过，覆盖设置持久化、新服务实例读取、Views / VirtualFolders / 根目录 Items 一致性、管理员写入限制、用户库权限与恢复默认。
- `TestEmbyLibraryCover`：相同临时数据库上执行通过。
- 前端 lint、TypeScript / Vite 构建与 `git diff --check` 通过。
- 浏览器回归脚本：`web/scripts/check-emby-library-display.mjs`；使用虚构账号和模拟 API，不操作真实账户或配置。
- 尚未验证真实 Emby 客户端；客户端自己的缓存或排序选项可能影响最终展示。
- 弹窗鼠标拖拽、键盘排序、焦点返回、取消、保存、失败保留、加载重试、普通用户入口隔离通过浏览器验证；深浅主题 390 / 768 / 1023 / 1024 / 1440 宽度无横向溢出。弹窗可访问性扫描未报告违规；渐变保存按钮对比度工具无法自动判定，已目视复核。
- 接口说明页同步了 Views / MediaFolders / VirtualFolders / 根目录 Items 描述；搜索、分类、空结果、展开、复制成功/失败和非管理员访问检查通过。等待主题过渡结束后，额外扫描仅剩既有“必填”红字的浅色对比度告警，相关样式本次未修改。
- 独立复核发现并修正：弹窗管理列表需 include_hidden；关闭焦点应返回入口按钮；后端持久化统一 JSON 字段名，避免大小写兼容解析与前端读取不一致。
- 临时 PostgreSQL 容器已停止并自动删除；测试数据仅在其内部的隔离 schema 中创建。
- 完整浏览器回归脚本最终执行成功，调试浏览器会话已关闭。代码独立复核及最终差异检查通过，无新增依赖或表结构变更。
