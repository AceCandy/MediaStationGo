# Emby 接口管理页实施计划

## 1. 核对接口契约

- [x] 从 `internal/handler/emby_routes.go`、`emby_routes_lowercase.go` 和相关处理器整理播放器接入接口清单。
- [x] 核对每项的鉴权、参数来源、状态码、响应字段、内容类型和支持层级。
- [x] 明确统一的 `/emby`、无前缀、大小写兼容规则以及只在特定前缀存在的例外。

验证：逐项关联到实际路由与处理器，不依据通用 Emby 文档补写项目未实现的行为。

## 2. 建立静态接口目录

- [x] 新增类型化 catalog，录入分类、接口元数据、参数、响应字段和脱敏示例。
- [x] 将完整行为和兼容占位明确标记，并避免为别名复制相同内容。

验证：TypeScript 类型检查可覆盖所有条目；人工抽查登录、Items、PlaybackInfo、视频流、进度和 Sessions。

## 3. 实现独立管理页

- [x] 新增页面壳、总览信息、搜索和分类筛选。
- [x] 实现接口条目收起态与详情区，展示参数、响应和示例。
- [x] 实现复制路径/示例及失败时的非阻塞反馈。
- [x] 处理空结果、长路径、长字段和移动端布局。

验证：搜索、筛选、展开、复制状态均可操作，动态内容不引起布局跳变或页面级横向滚动。

## 4. 接入路由与导航

- [x] 在 `appRoutes.tsx` 中将 `/admin/emby/interfaces` 投影到观看空间“我的”之后，并保留旧地址重定向。
- [x] 桌面侧栏和移动端底栏均按继承的管理员权限隐藏普通用户入口。

验证：管理员侧边栏在“我的”之后显示“播放器接口”并正确激活；移动端顺序一致；旧地址跳转；非管理员入口不可见且访问仍由 `admin-root` 保护。

## 5. 独立复核与质量检查

- [x] 按 PRD 重新核对需求、范围和后端事实，检查静态目录是否存在误导或遗漏。
- [x] 执行 `cd web && npm run lint`。
- [x] 执行 `cd web && npm run build`。
- [x] 执行 `git diff --check`。
- [x] 在 390x844、768x1024、1440x900 视口检查页面并记录未覆盖风险。

回滚点：本任务不修改数据与后端；删除新增页面/catalog 及路由项即可回滚。
