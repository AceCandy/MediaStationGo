# 实施计划

1. 精简任务中心红果 Tab。
   - 删除两个顶部按钮、专属取消处理函数及失效 import。
   - 核对任务列表和通用执行逻辑未变化。
2. 精简 TMDB/豆瓣/Bangumi 发现页。
   - 榜单选择工具栏左对齐。
   - 删除实际内容与加载骨架中的重复榜单标题。
   - 将实际内容和骨架统一为 `md` 及以上五列。
3. 精简并对齐红果发现页。
   - 删除页头、任务中心入口和资料管理折叠区。
   - 清理仅由这些区域使用的代码。
   - 保留分类/榜单、搜索、刷新、详情和触底加载；目录与骨架统一为五列。
   - 榜单模式移除重复名称，保留总数。
4. 独立复核和验证。
   - 检查 diff 只涉及上述展示行为。
   - 运行 `npm run lint`、`npm run build`、`node web/scripts/check-hongguo-discover.mjs` 和 `git diff --check`。
   - 不启动服务，不执行 Java 或 Go 全量测试。
