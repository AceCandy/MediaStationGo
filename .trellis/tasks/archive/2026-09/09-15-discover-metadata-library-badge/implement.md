# 实施与验证

1. 先读共享元数据、发现加载、路由权限和设计规范；定位权威作品标识与媒体可见性查询的复用点。保留所有红果下载未提交文件。
2. 实现 TMDb 作品详情安全投影、有界批量入库状态接口和管理员异步刷新接口；测试身份/类型、可见性、任意分集、STRM、无文件元数据、字段保留、图片/快照/演职员更新和非法输入。
3. 弹窗按需加载详情，补充资料和演职员；卡片增加入库标签，取消过期请求，失败状态可恢复。必要时只分离现有演职员展示，不重构媒体详情。
4. 独立复核全部变更；运行针对性 Go 测试、Web lint/build、git diff --check，浏览器验证电影/剧集、缺字段、失败重试、账户切换与两种主题多档宽度。
5. 记录已验证/未验证内容，关闭本次启动的服务，更新发现接口约定，不擅自提交或部署。

用户已批准并实施；未提交、未部署，保留任务待提交，不自动归档。

## 验证结果（2026-09-15）

以下为上一版验证结果，不代表本轮修订已实现或验收。

- PostgreSQL 专项测试及 `go test -race ./internal/repository ./internal/service ./internal/handler -run 'TestDiscover|TestAuthenticatedRouteSurfacesAreRegistered' -count=1` 通过；覆盖身份类型、任意分集 STRM、metadata-only、可见库与 NSFW、只读详情、权限和输入校验。
- Web `npm run lint`、`npm run build` 和 `git diff --check` 通过。
- `node scripts/check-discover-detail.mjs` 通过：模拟接口验证资料、安全链接、同 ID 类型区分、按需请求、失败重试、账户隔离、Escape 关闭；深浅主题 390/768/1024/1440 宽度无横向溢出。人工查看手机深色、桌面浅色截图。
- 独立只读复核未发现阻断缺陷；主线程点验 SQL、服务安全投影、handler 权限与共享展示组件差异。
- 未验证真实 TMDb 联网、生产部署、完整 JWT 成功路径端到端及全项目回归。入库标签代表数据库中存在可见媒体记录，不验证物理文件可播放；缺可靠关联时不显示豆瓣链接。
- 浏览器测试的图片使用本地静态资源，API 路由替换先移除旧规则和兜底，避免模拟响应遮挡。

## 本轮修订执行计划（用户已确认并实施）

1. 确认并复用库内作品投影、图片选择和翻译字段，增加本地优先/无本地回退测试，断言普通详情 GET 本地命中零 Provider 请求、管理员刷新走共享刷新逻辑和隐藏库隔离。
2. 最小提取详情页实际共用的只读展示块，替换弹窗自建样式，添加 backdrop 全幅渐变；不改媒体专有信息或管理行为。
3. 独立复核权限与时长口径，运行 Go 专项测试、Web lint/build，浏览器核验本地翻译、远程预览、详情页回归、双主题响应式和背景可读性。
4. 更新接口契约并记录未验证项，关闭测试服务和清理临时产物，不自动提交。

## 本轮修订验收

### 最新修正：metadata 与 media 解耦

用户明确确认：发现详情不检查媒体文件或库权限，仅用 TMDb ID＋作品类型读取共享 metadata；列表已入库标签仍查可见 media。已移除详情服务的可见性参数和媒体存在性查询。`TestDiscoverDetailUsesMetadataWithoutMedia` 覆盖电影/整剧零 media 时返回译文、图片、豆瓣关联且零远端请求，并断言该作品仍无已入库标签。PostgreSQL 专项测试及 race 检查通过，主线程独立复核调用链、精确类型匹配和无文件路径投影；本轮无前端改动，未重新执行浏览器验收或真实 TMDb 联调。后续历史验收中的“隐藏库资料回退”要求由这次明确决定覆盖。

- 共享 metadata（包括无 media 记录的电影/整剧）使用 canonical 作品资料、选定图片及统一演职员投影；保存的中文姓名/角色与详情页一致，演员在前。普通详情 GET 命中本地零 TMDb 请求，未配置 TMDb 也可读；管理员刷新时有效最新字段、图片、快照和演职员替换，缺项不会清空旧值，稳定角色译文保留。
- 独立只读复核指出远端回退混入隐藏库豆瓣关联；已删除该路径并新增断言，主线程复核修正后的服务分支和测试。
- 实际 PostgreSQL Go 专项及 race 检查通过，涵盖本地优先、远端回退、类型身份、权限、可见性、缺项、演职员原接口与路由注册。
- Web lint/build、发现弹窗浏览器脚本、series presentation 和 Douban binding 回归通过。浏览器验证 390/768/1024/1440 双主题，截图人工核验桌面深色和手机浅色；未出现横向溢出，背景渐变、译文、安全链接及错误重试正常。
- 原 series 静态回归脚本遗漏 DoubanBindingDialog mock，且仍假定管理员豆瓣徽章为外链；补齐 mock 并分别断言现有管理员绑定按钮和普通用户外链，未改对应产品行为。
- 未验证真实 TMDb 联网、生产部署、全项目测试以及完整 JWT 成功路径端到端。本地时长以元数据/快照为准，不等同于某一文件探测时长。
- 保持未提交，不自动归档；已有红果改动保留不动。
