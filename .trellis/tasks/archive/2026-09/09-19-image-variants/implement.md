# 实施与验证

1. 参数化处理模块与纯 Go 编码依赖；单测尺寸/格式/缓存/并发/边界。
2. 接入六类本地出口及远程冷/热缓存；更新 Emby 参数目录。
3. Web 统一 URL、背景尺寸和 SW 变体身份；运行脚本、lint、build。
4. 独立只读复核，修复确证问题；记录未验证项。

前一轮封面查询、Cookie、懒加载修改一并纳入本任务。用户已明确授权提交、推送、归档和更新 Spec。

## 实施记录

- 已实现共享变体服务、六类图片出口、远程冷/热/原图写失败出口、Web 默认参数和背景参数、SW 缓存身份及 Emby 目录。
- 已补充真实编码、透明、比例、EXIF、动图回退、缓存更新、并发、HEAD/304、非法参数与写失败测试。
- 独立只读复核：远程出口“绕过变体”的报告经主线程复核为误报，实际调用的是 ImageProxy 方法且冷/热测试通过；EXIF 静态图回退问题已改为方向校正并补测试；占位图维持原行为。
- Web URL 脚本、lint/build，Go 图片相关回归，race 与 CGO_ENABLED=0/nodynamic 测试通过。
- 未验证：真实 PostgreSQL、真实浏览器/播放器兼容和部署性能。没有启动调试服务，也未部署。
- 收尾按代码与 Spec、任务归档、会话记录的顺序分别提交，推送至 origin/main。任务直接在 main 完成，未创建 PR，归档使用脚本的非 PR 分支校验豁免。
