# 红果合集默认关系

用户要求：合集接口返回 101001（该剧集已下架）时，视为独立作品，使用请求的红果 source ID 作为合集 ID、季号 1；正常成功响应中有效合集的 season_index 缺失或非整数时默认为 1。

验收：上述结果作为成功保存，不继续重试；不修改作品和分集身份。其他业务码、错误作品身份、无效合集 ID、整数季号越界仍失败。缺少官方合集的普通成功响应保留现有空关系行为。错误原文和敏感信息不写入日志。

范围：合集解析器、已有解析测试和红果合集契约。无数据库迁移，无下载逻辑变更。通过解析回归测试、静态检查与独立复核验证；不调用真实上游或部署。

验证：`go test ./internal/hongguo -count=1`、`go vet ./internal/hongguo`、`git diff --check` 通过。实现后独立复核：默认值仍在完整 JSON 校验之后；普通成功响应先校验作品/合集身份；整数溢出及越界不被季号默认值掩盖；沿用成功保存路径清除待重试状态。未运行真实接口或数据库集成验证，未部署。
