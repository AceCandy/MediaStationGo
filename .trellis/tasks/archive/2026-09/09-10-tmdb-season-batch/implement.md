# 实施与验证

当前实施最新批准方案；保留此前待办可见性修复。

1. 提取单集基础字段解析；增加执行内整季复用与原始语言补缺。
2. 接入目录、复查、快照、图片批量入口；单项刷新维持原行为。
3. 测试请求数、中文保护、原始语言、缺集无单集请求、手动刷新及并发取消。
4. PostgreSQL 回归目录、复查、快照、图片及季演职员读取/迁移安全；独立复核并更新规范。
5. 关闭临时服务，不提交或部署；保留前一任务工作树修改。

## 实施记录

- 已接入整季执行内缓存、原始语言补缺、拆集及缺集 72 小时复查；手动单集刷新保持原接口。
- 原子保护同 ID 的历史完整快照；启动本地化不覆盖晚于快照更新的集文字。
- Web/Emby 单项、批量和人物筛选统一季级演职员；停止集级写入；启动迁移只删除集级关联。
- 人物补齐任务新增季候选，优先利用历史季快照；共享人物、头像和原始快照不删除。
- 已完成实现后的独立差异复核，补上 Emby 单集人物筛选、旧快照启动覆盖和历史季演职员补齐遗漏。

## 验证结果

- 真实 PostgreSQL 隔离 schema：整季/快照/目录/复查、Web/Emby 季演职员、迁移及历史补齐重点回归通过。
- `go test -race ./internal/service -run '^TestTMDbSeasonBatch' -count=1` 通过。
- `web` 下 `npm run lint`、`npm run build` 通过；`git diff --check` 通过。
- service/repository/handler/database 全量测试与 HEAD 临时基线均为同一组 85 个失败，无新增失败；没有扩大任务去修复原有失败。最后的启动快照时间保护另经相关全部本地化测试验证通过。
- 未验证：真实 TMDb 账号、浏览器视觉交互、真实 Emby 客户端和生产升级；未提交、未部署、未操作生产数据库。
- 缓存有界，跨大量季发生淘汰时允许重新请求；“最多两次”指一次缓存填充，不包含剧语言查询和网络重试。
- 历史关联物理清理在部署后的启动迁移执行；缺少季演职员时在补齐完成前返回空列表，不回退整剧。

## 收尾

- 用户已确认将待办可见性修复与整季优化分别提交，然后归档任务；不推送、不部署。
- 补全进度仍逐集展示：整季详情复用后，集元数据、快照、剧照和检查点仍逐集保存。现有日志无法确定每集耗时占比，也不能用于确认运行服务的版本。
