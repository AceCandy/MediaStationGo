# 扫描可靠性与性能

修复已确认的公共扫描和监听缺口，保留 NFO、红果、普通库的独立资料策略。

验收：访问异常不得删除媒体记录；目录读取失败必须报告并阻止该根目录清理；单文件监听失败退避重试且不覆盖新事件；新增目录已有文件可以被发现；普通库 NFO 事件更新本地提示且不失效未变化媒体的探测结果；刮削每次仅加载一个完整作品组，保留优先级、来源隔离和并发领取校验。

全量扫描仍按媒体指纹跳过普通库未变化文件。无名称自动搜索、新资料体系或播放行为变更。使用隔离 PostgreSQL 和针对性测试验证，独立复核后结束。
