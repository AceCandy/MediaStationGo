# 实施与验证

1. 修复遍历及删除保护，验证访问失败、目录丢失、部分成功不清理。
2. 完成监听退避、目录补扫及普通库侧车刷新，验证三种来源隔离、事件合并、重试上限及探测保留。
3. 收窄待刮削组读取，验证完整分组、空值、并发和大队列执行计划。
4. 运行针对性 Go/PostgreSQL 测试及 race/vet；独立审查范围内 diff，修复后只重复受影响检查。
5. 更新扫描相关规范与验收记录，关闭临时服务。不自动提交。

## 本轮实施结果

- 共享遍历传播访问/属性读取错误；部分根扫描失败仍上报错误，保留成功写入，失败根不清理。
- 监听删除区分不存在与访问异常，并确认所属库根可访问。
- 监听失败复用现有队列退避重试，30 秒起、上限 5 分钟、最多 5 次；保留新事件与侧车意图。
- 新目录补扫、目录删除重验、监听错误合并补偿；停用库不会被残留事件重新纳入监听。
- 普通库 NFO 变化更新提示，已确认身份与 probe 不变；未绑定文件重新 pending；正在刮削时延后刷新。
- 待刮削先取一个代表再读取完整组，两次查询均排除运行中的作品；来源变化导致整组回滚；取消只退回实际领取 ID。
- 新增三个 PostgreSQL 部分索引，没有新增依赖或另建任务队列。保留 NFO、红果与普通库的既有资料边界。

## 验证结果

真实 PostgreSQL 16 隔离数据库，数据库测试没有因缺少 DSN 而跳过。

- 当前工作树专项 `go test -race ./internal/service`：48 项通过，0 跳过、0 失败。
- 专项覆盖 `TestWatcher*`、`TestRemovePath*`、遍历/不可读目录、多根与未变文件、入库错误保留、`TestClaimPending*`、NFO、ID-only、红果导入隔离及播放身份。
- 真实 fsnotify 整目录移入/删除测试在隔离 worktree 中 `-race -count=5` 通过。
- 当前工作树 `go test ./internal/database -count=1` 全部通过，包含索引存在及重复迁移。
- 当前工作树 `go test ./internal/repository -run 'TestNFO|TestHongGuo.*Bind' -count=1` 通过。
- `go vet ./internal/service ./internal/database`、`gofmt`、`git diff --check` 通过。
- 万条待办真实 EXPLAIN ANALYZE 使用三个新增索引；测试队列每组两文件，领取查询每次至多返回两行，不再把全部待办载入内存。此结果不是生产环境耗时或恒定复杂度承诺。
- 三个独立 PostgreSQL 连接确定性交错领取，只有一个事务成功；另测两次读取间的晚到单集、来源变化回滚与空白分组取消回退。
- 两轮只读独立审查；最后报告的领取来源复查和取消回退问题已修复，并由专项竞态测试复验。

## 扩大回归与边界

- 扩大扫描/整理/探测/红果发现回归没有全绿。用干净 `0833cc5` 对照，播放探测、整理、旧扫描断言、旧刮削夹具等多项失败可复现；没有为本轮改动重写这些无关业务。
- 红果发现 `repeated_page_is_not_completion` 子项在扩大组合运行中出现一次失败，单独重复运行通过，尚未进一步定位；不宣称全部扩大回归失败都已逐项归因。
- 主工作树存在其他会话的持续改动，曾导致编译期间文件消失；复测已通过，未覆盖或提交其他会话文件。
- 尚未做生产 NAS 断连/恢复、长期压力、百万文件规模及真实外部供应商验收。首次升级建索引耗时未在生产规模验证。
- 根目录仍存在但挂载已退化为空目录，不能只靠 stat 判定挂载健康；这不属于本轮“不存在/访问错误”保护能证明的场景。
- 重试到达上限后依赖下一次文件事件或扫库；未引入持久化监听事件队列。普通库图片单独变化、未变媒体全扫重读侧车均未扩大支持。
- 已按用户要求提交为 `12ee325` 并归档；其他会话的修改未纳入提交。
- 临时 PostgreSQL 已停止并移除；两份临时 worktree 已清理，清理前已确认验证源码与主工作区一致，主工作区实现完整保留。
