# Design

- 新增管理员 canonical metadata 豆瓣搜索/绑定接口，不复用会重绑文件的通用 scrape/apply。
- 搜索复用联想候选，有界读取 subject 详情确认类型；未知类型不可应用。
- 应用先验证详情、实际类型和强制确认，下载图片后，在同一事务保存标识、缺失字段、图片关联及快照。
- MetadataIdentifier 增加豆瓣实际类型；空值保持旧行为，仅人工绑定流程写入。
- 前端复用候选列表、ModalShell 与确认弹窗；元数据编辑不再直接改豆瓣 ID。
- 保留原字段补齐和图片选择规则、任务 key、冷却及游标。
