# 实施计划

1. 调整豆瓣 URL 规范化
   - 官方大图 URL 删除已知 `imageView2/...` 查询串。
   - `ResolveArtworkURL` 不再持久化配置 CDN；提取最小的 CDN WebP 投影逻辑供图片代理复用。
   - 更新 URL、搜索、发现、详情和候选来源测试。

2. 在共享图片代理增加 CDN → 官方源回退
   - 正向缓存键保持官方 URL。
   - 未命中缓存时读取当前 Douban 配置并临时生成 CDN URL；失败后请求官方 URL。
   - 仅最终官方 404 写负缓存；同 URL 404 不继续无意义的客户端/curl 尝试。
   - 增加 CDN 成功、CDN 失败回退、配置热切换、网络失败不缓存及 404 缓存测试。

3. 验证与运行时清理
   - 运行豆瓣 URL、图片代理和豆瓣补齐相关测试，随后执行 service 包测试、`go vet ./...` 和 `git diff --check`。
   - 独立复核持久化 URL、缓存键、回退顺序和错误分类。
   - 删除用户本次提供的 `p454132922` CDN URL 所对应的精确 `.fail.direct` 标记，并确认未删除其他缓存。
